# Copy to .env and fill in. Never commit the real .env.
# Referenced by name from config.yml's `connection_env` / SMTP settings —
# see config-schema.md §2.

WAREHOUSE_DSN=postgresql://user:password@host:5432/dbname

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
